@model IEnumerable<ProfsCursoMtoM_WithPayload.Models.Professor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sobrenome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataContratacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sobrenome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataContratacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProfessorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProfessorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProfessorId })
        </td>
    </tr>
}

</table>
